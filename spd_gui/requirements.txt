PySide6>=6.5
pyusb>=1.2.1
# No Windows, o pyusb precisa da libusb-1.0.dll. O pacote abaixo já traz
# a DLL embutida, evitando ter que instalá-la manualmente. Só é necessário
# se você for usar o modo de conexão USB (não é preciso para o modo COM):
libusb>=1.0.27
# Necessário para o modo de conexão por porta COM/serial (alternativa ao
# modo USB que não exige trocar o driver com o Zadig):
pyserial>=3.5
